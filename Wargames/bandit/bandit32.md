# حل مستوى Bandit 32 → 33
**التاريخ:** 2025-10-19
## 🔍 فهم المشكلة

أنت داخل **UPPERCASE SHELL** - وهو غلاف مقيد يعمل كالتالي:
- يأخذ أي أمر تكتبه
- **يحوله إلى أحرف كبيرة كاملة**
- ثم يحاول تنفيذ الناتج

### المشكلة:
- `cat` تتحول إلى `CAT` → لا يوجد أمر بهذا الاسم ❌
- `ls` تتحول إلى `LS` → لا يوجد أمر بهذا الاسم ❌
- `sh` تتحول إلى `SH` → لا يوجد أمر بهذا الاسم ❌

## 🎯 الحل الأصلي: استخدام `$0`

```bash
>> $0
```

### شرح ما يحدث:

**ما هو `$0`؟**
- `$0` هو **متغير خاص** في Linux
- يحتوي على **اسم البرنامج الحالي الذي يعمل**
- في حالتنا: `$0` = اسم الـ UPPERCASE SHELL نفسه

**كيف يعمل؟**
1. تكتب `$0` في الـ UPPERCASE SHELL
2. النظام يتعرف على `$` كبادئة متغير
3. **لا يحول المحتوى إلى uppercase** لأنه متغير
4. يقوم بتوسيع المتغير `$0` إلى اسم الـ shell
5. ينفذ اسم الـ shell → يفتح **جلسة shell عادية جديدة**

## 🚀 الحل المحسّن والخطوات الكاملة

### الخطوة 1: الدخول إلى المستوى 32
```bash
ssh bandit32@bandit.labs.overthewire.org -p 2220
```
**كلمة المرور:** `rmCBvG56y58BXzv98yZGdO7ATVL5dW8y`

### الخطوة 2: الهروب من UPPERCASE SHELL
```bash
>> $0
```

### الخطوة 3: التأكد من الخروج من البيئة المقيدة
```bash
whoami
```
✅ الناتج المتوقع: `bandit33`

### الخطوة 4: الحصول على كلمة المرور
```bash
cat /etc/bandit_pass/bandit33
```

## 🎓 الدروس المستفادة

1. **متغيرات Shell قوية**: `$0`, `$1`, `$@` وغيرها يمكن استخدامها بطرق إبداعية
2. **الهروب من البيئات المقيدة**: ابحث عن الثغرات في تفسير الأوامر
3. **الفهم العميق**: معرفة آلية عمل Shell تساعد في حل المشاكل غير العادية
4. **المرونة في التفكير**: هناك دائماً أكثر من طريقة لحل المشكلة

## 🔐 لماذا يعمل `$0`؟

- **UPPERCASE SHELL** مصمم لتحويل الأوامر فقط
- **المتغيرات** لا تعتبر أوامر بل قيم
- عندما ينفذ `$0`، يشغل نسخة جديدة من الـ shell
- النسخة الجديدة **ليست مقيدة** وتعمل بشكل طبيعي


