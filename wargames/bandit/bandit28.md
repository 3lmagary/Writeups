# حل مستوى Bandit 28 → 29

**التاريخ:** 2025-10-19
**المصدر:** OverTheWire — Bandit

---

## 🎯 الهدف (Goal)

> استنساخ مستودع git المتاح عبر SSH على البورت `2220` للمستخدم `bandit28-git` ثم البحث في تاريخ الـrepo (commits) لمعرفة كلمة المرور للمستوى التالي (`bandit29`).

---

## 🔧 الأدوات المستخدمة (Tools Used)

* `git` — لاستنساخ المستودع واستعراض السجل (commits).
* `ssh` — ضمن عملية `git clone` للاتصال عبر البورت 2220.
* `cat`, `git show`, `git log` — لعرض الملفات والإصدارات السابقة.

---

## 🚀 خطوات الحل (Solution Steps)

1. أنشأت مجلد مؤقت على الجهاز المحلي ودخلت عليه:

```bash
mkdir /tmp/lsjkdfa && cd /tmp/lsjkdfa
```

2. استنسخت الريبو عبر SSH على البورت 2220:

```bash
git clone ssh://bandit28-git@bandit.labs.overthewire.org:2220/home/bandit28-git/repo
cd repo
```


3. عرضت محتوى `README.md` الحالي فكان مخفّياً (`password: xxxxxxxxxx`):

```bash
cat README.md
```

4. عرضت سجلّ الـgit (commits) ولاحظت عدة commits:

```bash
git log
```

5. عرضت نسخة `README.md` في الكوميت الذي يحتوي البيانات المفقودة (commit hash):

```bash
git show 8b7c651b37ce7a94633b7b7b7c980ded19a16e4f:README.md
```

---

## 🔓 كلمة المرور المستخرجة (Password for bandit28)

```
4pT1t5DENaYuqnqvadYs1oE4QLCdjmJ7
```

---


