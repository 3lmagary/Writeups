# حل مستوى Bandit 27 → 28

**التاريخ:** 2025-10-19
**المصدر:** OverTheWire — Bandit

---

## 🎯 الهدف (Goal)

> هناك مستودع `git` متاح عبر SSH على `ssh://bandit27-git@bandit.labs.overthewire.org` على البورت `2220`.
> عليك استنساخ الـrepo والعثور على كلمة المرور للمستوى التالي (bandit28).

---

## 🔧 الأدوات المستخدمة (Tools Used)

* `git` — لاستنساخ المستودع والتفتيش في سجلّاته (commits).
* `ssh` — مُضمّن في عملية `git clone` عند الاتصال عبر SSH.
* `cat`, `ls`, `cd` — للاستعراض.

---

## 🚀 طريقة الحل (Solution Steps)

> **مهم:** لا تقم بالتنفيذ من داخل جلسة SSH على خادم Bandit نفسه — نفّذ الأوامر على جهازك المحلي (كما بالمثال أدناه).

### 1. أنشئ مجلد مؤقت واعمل داخله

```bash
mkdir /tmp/3lmagary && cd /tmp/3lmagary
```

### 2. استنساخ المستودع عبر SSH على البورت 2220

```bash
git clone ssh://bandit27-git@bandit.labs.overthewire.org:2220/home/bandit27-git/repo
```

سَيُطالَب بكلمة مرور المستخدم `bandit27-git` — استخدم كلمة مرور `bandit27` (من المستوى السابق).
(في المثال الذي تم تنفيذه: `upsNCc7vzaRDx6oZC6GiR6ERwe1MowGB`)

### 3. ادخل مجلد الريبو واطّلع على الملفات

```bash
cd repo
ls
cat README
```

---

## 🔓 كلمة المرور المستخرجة (Password for bandit28)

```
Yz9IpL0sBcCeuG7m9uQFt8ZNpS4HZRcN
```

---

## 💡 ملاحظات (Notes)

* سبب عدم القدرة على عمل `git clone` من داخل خادم Bandit هو أن الاتصال من `localhost` إلى نفس الخادم عبر البورت 2220 محظور (لذلك نفّذ الاستنساخ من جهازك المحلي).

---

