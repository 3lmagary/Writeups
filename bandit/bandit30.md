# حل مستوى Bandit 30 → 31

**التاريخ:** 2025-10-19
**المصدر:** OverTheWire — Bandit

---

## 🎯 الهدف (Goal)

استنساخ مستودع `git` للمستخدم `bandit30-git` والبحث في تاريخ المستودع (commits/branches/tags/objects) لاستخراج كلمة المرور للمستوى التالي `bandit31`.

---

## ✅ النتيجة النهائية (Final result)

تم العثور على كلمة المرور داخل كائن `blob` المشار إليه بالـtag `secret`.

---

## الخطوات التي اتّبعتها (Steps taken)

1. استنسخت المستودع على جهازك المحلي (لا تعمل clone من داخل جلسة SSH على خادم Bandit):

```bash
git clone ssh://bandit30-git@bandit.labs.overthewire.org:2220/home/bandit30-git/repo

cd repo
```

2. راجعت README الحالي لكنه فارغ:

```bash
cat README.md
# just an epmty file... muahaha
```

3. استعرضت المراجع (refs) ولاحظت وجود `tag` باسم `secret`:

```bash
git show-ref
# خروج: refs/tags/secret -> 84368f3a7ee06ac993ed579e34b8bd144afad351
```

4. حدّدت نوع الكائن المشار إليه بالهاش ثم عرضت محتواه:

```bash
git cat-file -t 84368f3a7ee06ac993ed579e34b8bd144afad351
# خروج: blob

git show 84368f3a7ee06ac993ed579e34b8bd144afad351
# خروج: (نص الباسورد)
```

---

## ملاحظات تفسيرية (Why this worked)

* الـ`tag` قد يشير إلى `commit`, `tag` موضوع، أو مباشرةً `blob`. في هذه الحالة كان `blob` يحتوي النص المطلوب، لذلك عرضنا محتوى الـblob مباشرةً باستخدام `git show`.

---

## أوامر مساعدة سريعة لإعادة التحقق

```bash
# عرض كل الrefs
git show-ref

# عرض نوع أي object
git cat-file -t <hash>

# عرض محتوى أي object
git show <hash>

# لو كان tag annotated
git show refs/tags/secret
```

---
<mark style="background: #FFB86CA6;">لا تنسي الصلاه علي النبي محمد </mark>

